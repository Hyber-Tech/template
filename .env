# hardcode shared variables, and use env.local for unshareable secrets

SUPABASE_PROJECT_REF="abcdefg"
SUPABASE_PROJECT_LABEL="example-project"

# The only schema this project builds in, and the prefix every object carries.
PROJECT_SCHEMA="prefixe_project"
PROJECT_PREFIX="prefixe_project_"
